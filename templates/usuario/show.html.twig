{% extends 'admin/base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.Nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ usuario.Apellidos }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usuario.email }}</td>
            </tr>
            <tr>
                <th>Pass</th>
                <td>{{ usuario.pass }}</td>
            </tr>
            <tr>
                <th>Apodo</th>
                <td>{{ usuario.apodo }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ usuario.created ? usuario.created|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Usuario Activo</th>
            {% if usuario.isactive  %}
                <td>Sí</td>
            {% else %}
                <td>No</td>
            {% endif %}
            </tr>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuario_index') }}">back to list</a>

    <a href="{{ path('usuario_edit', {'id': usuario.id}) }}">edit</a>

    {{ include('usuario/_delete_form.html.twig') }}
{% endblock %}
